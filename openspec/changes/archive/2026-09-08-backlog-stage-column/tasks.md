## 1. Таблица порядка (раздел 3)

- [x] 1.1 Заменить шапку таблицы на `№ | ID | Класс | Зависимость | Не позже | Стоим. | Ломает | Граница общего/личного | Статус` и убедиться, что число ячеек в каждой из 24 строк совпадает с числом колонок шапки
- [x] 1.2 Заполнить таблицу по образцу ниже; проверить, что для каждой строки все ID из «Зависимость» стоят **выше** неё, а все ID из «Не позже» — **ниже**

  | № | ID | Класс | Зависимость | Не позже | Стоим. | Ломает | Граница общего/личного | Статус |
  | --- | --- | --- | --- | --- | --- | --- | --- | --- |
  | 1 | `project-skeleton` | — | — | — | S | нет | не затрагивает | ✅ |
  | 2 | `glossary-english` | — | — | `auth-roles` | S | нет | не затрагивает | ✅ |
  | 3 | `auth-roles` | — | `project-skeleton` | — | M | нет | **задаёт** | ✅ |
  | 4 | `backlog-stage-column` | Л | — | — | S | нет | не затрагивает | |
  | 5 | `file-storage` | — | — | — | S | нет | не затрагивает | |
  | 6 | `rubricator-tree` | — | `auth-roles` | — | M | нет | общее | |
  | 7 | `library-dictionaries` | — | `auth-roles` | — | S | нет | общее | |
  | 8 | `problem-catalog` | — | `rubricator-tree`, `library-dictionaries`, `file-storage` | — | M | нет | общее | |
  | 9 | `library-search` | — | `problem-catalog` | — | M | нет | общее | |
  | 10 | `theory-materials` | — | `rubricator-tree`, `file-storage` | — | S | нет | общее | |
  | 11 | `rubricator-restructure` | — | `rubricator-tree`, `problem-catalog` | `mastery-marks` | L | **да** | общее → личное | |
  | 12 | `students-groups` | — | `auth-roles` | — | S | нет | личное | |
  | 13 | `assignments` | — | `students-groups`, `library-search`, `theory-materials` | — | M | нет | личное | |
  | 14 | `deployment-backup` | — | — | `submission-review` | M | нет | не затрагивает | |
  | 15 | `submission-review` | — | `assignments`, `file-storage` | — | M | нет | личное | |
  | 16 | `mastery-marks` | — | `submission-review`, `problem-catalog` | — | M | нет | личное | |
  | 17 | `mastery-views` | — | `mastery-marks`, `rubricator-tree`, `library-search` | — | M | нет | личное | |
  | 18 | `method-edit-impact` | — | `mastery-marks` | — | S | **да** | общее → личное | |
  | 19 | `teacher-proposes` | — | `problem-catalog`, `auth-roles` | — | M | нет | общее | ждёт условия |
  | 20 | `mastery-history` | — | `mastery-marks` | — | S | **да** | личное | ждёт условия |
  | 21 | `topic-merge` | — | `rubricator-restructure` | — | M | **да** | общее → личное | ждёт условия |
  | 22 | `schedule` | — | `students-groups` | — | L | нет | личное | отложено |
  | 23 | `silent-failure-principle` | Д | — | — | S | нет | не затрагивает | ✅ |
  | 24 | `backlog-skill-arcs` | Л | `backlog-stage-column` | — | L | нет | не затрагивает | ждёт условия |

- [x] 1.3 Сверить с прежней таблицей, что изменилось ровно то, что перечислено ниже, и ничего сверх; проверка — построчным `git diff`:
  - `deployment-backup` переехал с 17-го места на 14-е, `submission-review`, `mastery-marks`, `mastery-views` сдвинулись на 15–17; строки 1–13 и 18–23 сохранили номера;
  - у `library-dictionaries` появилась зависимость `auth-roles`, которой не было в карточке: словари ведёт Администратор, то есть без ролей их не построить (`backlog.md:319`);
  - у `rubricator-restructure` появилась дуга «не позже `mastery-marks`» — бывшее жёсткое правило раздела 2;
  - у `glossary-english` дуга «не позже `auth-roles`» — бывшая строка «Связи» карточки (`backlog.md:157`);
  - статусы `ждёт условия` у строк 19–21 взяты из таблицы отложенного `context/strategy.md`, где у каждой записан проверяемый триггер; `отложено` у `schedule` — из [ADR-0019](../../context/adr/0019-raspisanie-otlozheno.md), решение владельца;
  - `method-edit-impact` статуса **не получает**: его карточка (`backlog.md:587`) говорит «берётся после `mastery-marks`, когда есть на чём проверить» — это зависимость, а не откладывание; прежнее «отложено» в колонке «Этап» было неточным.

## 2. Правило упорядочивания (раздел 2)

- [x] 2.1 Переписать вводную и таблицу ключей: порядок — не сортировка, а система ограничений. Проверка: в разделе не осталось слов «этап», «лексикографический» и ключа «Связи»
- [x] 2.2 Изложить ограничения в таблице ключей — «Зависимость» (без этого не построить), «Не позже» (после этого поздно или дорого), «Класс» (дефект впереди постройки), «Стоимость» (среди равных дешёвое раньше), «Ломающесть» (настолько поздно, насколько позволяют дуги, и одним окном). Проверка: каждый ключ назван колонкой таблицы раздела 3 или объяснено, почему колонки у него нет
- [x] 2.3 Записать, что равные по ключам элементы стоят в произвольном порядке и таблица фиксирует один из допустимых; привести пример ничьей `file-storage` / `library-dictionaries` / `students-groups`. Проверка: правило не обещает единственного порядка
- [x] 2.4 Удалить жёсткое правило про метки, убедившись, что его причина («иначе отметки накопятся до того, как определено, что с ними делать при перестройке дерева») перенесена в карточку `rubricator-restructure`
- [x] 2.5 Сохранить оба прежних исключения (блокер поднимается наверх; мелкие уточнения подбираются попутно) и правило неизменности номеров. Проверка: оба абзаца на месте и не ссылаются на этапы

## 3. Как пользоваться (раздел 1)

- [x] 3.1 В таблице адаптаций заменить строку про класс `Л/Б/Т/Д`: класс применим и имеет колонку, но подчинён дугам, тогда как в навыке `backlog-maintenance` он первый ключ сортировки. Проверка: строка называет отличие от навыка, а не отменяет класс
- [x] 3.2 Заменить абзац «Когда появится код… дефект идёт впереди постройки того же этапа» формулировкой без этапов. Проверка: слова «этап» в разделе 1 не осталось
- [x] 3.3 Добавить правило чтения: брать можно верхний элемент с пустым «Статусом», у которого все зависимости стоят с ✅; значения «можно брать» в таблице нет, оно выводится. Проверка: правило объясняет, почему `deployment-backup`, стоящий четырнадцатым, доступен уже сейчас
- [x] 3.4 Описать два значения статуса — `отложено` (снимается решением владельца) и `ждёт условия` (триггер в `context/strategy.md`, столбец «Состояние», либо в самой карточке). Проверка: для каждой из строк 19, 20, 21, 22 и 24 понятно, откуда взято значение

## 4. Карточки (раздел 4)

- [x] 4.1 Пройти все 23 прежние карточки и убрать из строки «Связи» дуги обоих направлений: «Опирается на …», «Нужен …», «Блокирует …», «После …», «Парный к … берётся после …». Проверка: `grep -n 'Опирается на\|Блокирует\|Нужен `' openspec/backlog.md` в разделе 4 не находит ничего
- [x] 4.2 Сохранить в «Связях» то, что дугами не выражается: условия возврата (`topic-merge`, `mastery-history`), парность (`rubricator-restructure` ↔ `method-edit-impact`), указания на смежные элементы (`library-search` → подбор по пробелам), правило «берётся попутно» у `silent-failure-principle`. Проверка: ни один из этих смыслов не потерян при сверке с `git diff`
- [x] 4.3 Удалить строку «Связи» там, где после чистки не осталось содержания. Проверка: пустых строк вида `**Связи.**` без текста в файле нет
- [x] 4.4 В карточку `rubricator-restructure` перенести причину дуги «не позже `mastery-marks`» — цену введения правил перестройки на живых данных со ссылкой на `context/strategy.md`
- [x] 4.5 В карточке `deployment-backup` заменить самостоятельное суждение «может выполняться параллельно личному контуру» ссылкой на `context/strategy.md`, раздел «Порядок работ и почему он такой». Проверка: карточка больше не утверждает момент от себя, и противоречия с таблицей возникнуть неоткуда
- [x] 4.6 Перенумеровать заголовки карточек вслед за переездом `deployment-backup`: `### 14. submission-review` → `### 15.`, `### 15. mastery-marks` → `### 16.`, `### 16. mastery-views` → `### 17.`, `### 17. deployment-backup` → `### 14.`, и переставить сами карточки в этом порядке. Проверка: последовательность `grep -n '^### [0-9]'` идёт 1…24 без пропусков, и ID в заголовке каждой карточки совпадает с ID той же строки таблицы
- [x] 4.7 Привести отметку о закрытии к одной конвенции — той, что задаёт навык `backlog-maintenance` (§4): галочка живёт в строке статуса, а не в заголовке. Убрать `· ✅` из заголовков карточек 1, 2, 3 (`backlog.md:93`, `:120`, `:167`) и дописать каждой блок `> ✅ Выполнено ДД.ММ.ГГГГ. Change: […]` — с датой закрытия и ссылкой на архив изменения:
  - `project-skeleton` — 07.09.2026, `openspec/changes/archive/2026-09-07-project-skeleton/`;
  - `glossary-english` — 07.09.2026, `openspec/changes/archive/2026-09-07-glossary-english/`;
  - `auth-roles` — 07.09.2026, `openspec/changes/archive/2026-09-07-auth-roles/`.

  В резюме каждого блока — короткое «что из карточки подтвердилось или разошлось», по формату навыка; материал брать из `proposal.md` соответствующего архивного изменения. Проверка: `grep '^### ' openspec/backlog.md` не содержит ни одной галочки, а число блоков `> ✅` равно числу ✅ в колонке «Статус»
- [x] 4.8 Завести двадцать четвёртую карточку `backlog-skill-arcs` по составу ниже. Проверка: карточка содержит все обязательные поля формата и её параметры совпадают со строкой 24 таблицы

  - **Заголовок:** `### 24. \`backlog-skill-arcs\` · Л · L · не ломает`
  - **Симптом.** Навык `backlog-maintenance` предписывает хранить порядок дважды: ранжирующие ключи — в таблице, дуги — прозой в карточке. Обещанный им «лексикографический ключ сортировки» единственного порядка не даёт. Проектный `backlog.md` и навык разъезжаются молча, и проверить их соответствие нечем.
  - **Доказательство.** `SKILL.md:41` — в перечне колонок таблицы дуг нет; `SKILL.md:47` — «ключ сортировки лексикографический», не выполняется на ничьих; `SKILL.md:59` — ключ «связи» вычисляется из прозы карточки; `SKILL.md:82` — заголовок карточки дублирует четыре колонки таблицы, включая сдвигающийся номер; `SKILL.md:98` — «Связи» велено писать прозой. Плюс: копий навыка две и они уже разошлись — `~/.claude/skills/backlog-maintenance/` версии 1.0 против `~/projects/opentest-skills/.claude/skills/backlog-maintenance/` версии 1.1 с разделом про `backlog-grooming`.
  - **Зачем.** Пока метод живёт вне проекта и без проверки, конвенция, введённая `backlog-stage-column`, продержится ровно до первой невнимательной правки — и сломается тихо, как уже ломалась.
  - **Что входит.** Копия навыка в `.claude/skills/backlog-maintenance/` как объявленный форк (`forked_from`, `forked_at` в шапке); перенос в него колонок дуг, порядка как системы ограничений, значений статуса и одной конвенции отметки о закрытии; новый раздел «Инварианты файла» с шестью проверками; номер формата в шапке `openspec/backlog.md` и знание навыка о том, какой формат он умеет; скрипт-проверялка по образцу `openspec/context/adr/build_index.py`; два хука в `.claude/settings.json` — `PostToolUse` по `openspec/backlog.md` и `SessionStart`; правка `CLAUDE.md` о том, какая копия навыка для проекта главная; запись журнала `area: процесс` про форк.
  - **Развилки.** Что делать с заголовком карточки (`SKILL.md:82`), дублирующим номер, класс, стоимость и ломающесть: убрать номер, объявить истиной таблицу или оставить как есть. Предпочтительный не помечен — выбор владельца.
  - **Граница общего и личного.** Не затрагивает: данных у элемента нет, правятся навык, документ очереди и оснастка.
  - **Связи.** Уведомление, а не запрет: хуки не останавливают ни сессию, ни правку — решение владельца от 08.09.2026. Точную семантику кодов возврата хуков сверить с документацией при реализации, а не по памяти.
  - **Признак готовности.** Навык не предписывает хранить порядок дважды; описанный им ключ даёт проверяемый результат на очереди с ничьими; раздел инвариантов позволяет проверить чужой `backlog.md`, не зная его истории; сломанный формат виден в тот же ход правки; в `CLAUDE.md` сказано, какая копия навыка главная.

- [x] 4.9 Снять со своей карточки (`backlog-stage-column`) вводную сноску «Начинать с `/opsx:explore`» и проставить отметку выполнения по формату навыка: `> ✅ Выполнено ДД.ММ.ГГГГ. Change: …` с кратким изложением взятого подхода

## 5. Проектные документы

- [x] 5.1 Поправить `CLAUDE.md`, раздел «Ведение бэклога»: там записан прежний ключ «зависимость → стоимость → связи → ломающесть» и утверждение «а не классы `Л/Б/Т/Д`». Проверка: раздел описывает две колонки дуг и применимость класса, оставаясь маршрутизатором — метод по-прежнему живёт в навыке и разделах 1–2 бэклога
- [x] 5.2 Убедиться, что прочие документы контекста не затронуты: `glossary.md`, `domain-model.md`, `scenarios.md`, `architecture.md`, `standards.md`, `antipatterns.md` и журнал решений остаются как есть; `strategy.md` не правится — её доктрина порядка используется как источник и не переписывается. Проверка: `git status` показывает изменёнными только `openspec/backlog.md`, `CLAUDE.md` и файлы изменения

## 6. Проверка целиком

- [x] 6.1 Проверить, что номер и ID в заголовке каждой карточки совпадают с той же строкой таблицы, а отметка о закрытии стоит ровно в двух местах — колонка «Статус» и блок `> ✅` — и ни в одном заголовке
- [x] 6.2 Прочесть таблицу и все 24 карточки подряд и убедиться, что ни одна карточка не утверждает порядка, которого нет в таблице, и наоборот — в частности, у `deployment-backup`, `glossary-english`, `rubricator-restructure` и `mastery-marks`
- [x] 6.3 Проверить, что каждый ID, упомянутый в колонках «Зависимость» и «Не позже», существует в колонке ID: выписать оба множества и сравнить
- [x] 6.4 Прогнать `npx -y @fission-ai/openspec@latest validate --changes` и убедиться, что изменение проходит проверку при `skip_specs: true`
