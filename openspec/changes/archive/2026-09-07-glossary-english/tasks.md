## 1. Словарь

- [x] 1.1 Добавить в `openspec/context/glossary.md` столбец «В коде» во все
      четыре таблицы терминов (библиотека, личный контур, роли, вышедшие
      из употребления — последней столбец не нужен, но об этом сказать явно);
      проверка: у каждого из 18 действующих терминов заполнено ровно одно имя,
      имена совпадают с таблицей Р2 и списком прямых переводов из proposal.md
- [x] 1.2 Записать в `glossary.md` правило образования имён в базе (Р3 design.md):
      единственное число и `snake_case` от имени класса, завершающее
      подчёркивание для зарезервированных слов SQL, имена полей, внешних ключей,
      связующих таблиц и пакетов областей; проверка: по правилу выводится
      `user_`, `group_`, `assignment_batch`, `problem_topic` без обращения
      к человеку
- [x] 1.3 Записать в `glossary.md` имена перечислений и их значений (Р4):
      `ExamPart`, `MasteryStatus`, `Role`; проверка: четыре статуса владения
      из `glossary.md:29` отображены один в один, `UNKNOWN` отделён
      от `NOT_MASTERED`
- [x] 1.4 Добавить в `glossary.md` краткие пояснения к шести именам,
      не совпадающим с дословным переводом (`Problem`, `SolutionMethod`,
      `Taxonomy`, `ExamPart`, `AssignmentBatch`, `StudentWork`), со ссылкой
      на ADR-0025 за подробным разбором; проверка: читатель словаря понимает
      причину имени, не открывая журнал
- [x] 1.5 Добавить в `glossary.md` оговорку о том, что идентификаторы бэклога
      (`rubricator-tree`, `rubricator-restructure`, `submission-review`) —
      исторические имена работ,
      а не имена в коде (Р5); проверка: оба расхождения названы поимённо

## 2. Стандарты и журнал

- [x] 2.1 Поправить раздел «Язык» в `openspec/context/standards.md`: ссылка
      на столбец «В коде» словаря вместо единственного примера `Assignment`,
      упоминание правила имён таблиц; проверка: пример `Assignment` сохранён
      и не противоречит словарю
- [x] 2.2 Завести `openspec/context/adr/0025-imena-terminov-v-kode.md`
      по `TEMPLATE.md` со статусом `Предложено`, `area: техника`: контекст,
      решение (перевод, шесть особых имён, правило имён таблиц), причины,
      отвергнутые альтернативы (транслитерация, `Task`, `Rubricator`, `USE`,
      `Distribution`, `Submission`, множественное число, кавычки), последствия,
      связи; проверка: разделы шаблона заполнены все
- [x] 2.3 Пересобрать индекс журнала: `python3 openspec/context/adr/build_index.py`;
      проверка: `python3 openspec/context/adr/build_index.py --check`
      завершается без ошибок, запись 0025 видна в таблице `adr/README.md`

## 3. Сверка и закрытие

- [x] 3.1 Сверить принятые имена с идентификаторами `openspec/backlog.md`
      и с примером `standards.md:91`; проверка: расхождений ровно три
      (`rubricator-tree`, `rubricator-restructure`, `submission-review`),
      все объяснены задачей 1.5,
      прочие идентификаторы совпадают с именами словаря
- [x] 3.2 Прогнать `npx -y @fission-ai/openspec@latest validate --all --strict`;
      проверка: изменение `glossary-english` проходит проверку при
      `skip_specs: true`
- [x] 3.3 Отметить элемент `glossary-english` в `openspec/backlog.md` как ✅
      (строка таблицы порядка и заголовок карточки); проверка: отметка стоит
      в обоих местах, номер элемента не изменился

## 4. Документы контекста и CLAUDE.md

- [x] 4.1 Затрагиваемые документы названы и правятся задачами выше:
      `glossary.md` (1.1–1.5), `standards.md` (2.1), `adr/` (2.2–2.3),
      `backlog.md` (3.3). **Не затрагиваются:** `domain-model.md`,
      `scenarios.md`, `architecture.md`, `antipatterns.md`, `strategy.md`
      и `CLAUDE.md` — изменение не меняет ни модель, ни сценарии, ни стек,
      ни маршрутизацию; проверка: `grep` по этим файлам не находит утверждений
      об именах в коде, которые новый словарь делал бы неверными

## 5. Тесты изоляции

- [x] 5.1 Тестов нет и не требуется: изменение не создаёт кода, данных и правил
      фильтрации по владельцу — правятся только документы `openspec/context/`
      (Р6 design.md); проверка: `git diff --name-only` содержит только файлы
      `openspec/`
