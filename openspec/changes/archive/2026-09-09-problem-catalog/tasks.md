## 1. Схема

- [x] 1.1 Завести миграцию `0004-problem-catalog.yaml`: таблица `problem` —
  `id` BIGINT autoincrement (он же номер Задачи), `caption` VARCHAR(300)
  nullable, `exam_part` VARCHAR(16) NOT NULL (имя константы перечисления,
  как `role` в `0001-users-and-roles.yaml`), `condition_file_key`
  и `solution_file_key` VARCHAR(200) NOT NULL — обязательность обоих PDF
  выражена схемой, а не только сервисом. Проверка:
  `mvn test -Dtest=MigrationsOnStartupTest` проходит, таблица есть в чистой базе
- [x] 1.2 Добавить в ту же миграцию три таблицы связей: `problem_topic`
  (`problem_id`, `topic_id` → `taxonomy_node(id)`), `problem_solution_method`
  (`problem_id`, `solution_method_id`), `problem_characteristic`
  (`problem_id`, `characteristic_id`); у каждой составной первичный ключ
  и внешние ключи на обе стороны. Имена выведены из имён классов
  (`glossary.md`, «Имена в коде и в базе»), выбирать нечего. Проверка:
  миграция накатывается, попытка сослаться на несуществующий узел или запись
  словаря отклоняется базой
- [x] 1.3 Проверить на базе разработки, что миграция накатывается **поверх
  данных** предыдущей версии, а не только на чистую: `mvn spring-boot:run`
  без `docker compose down -v` (`CLAUDE.md`, «Команды сборки и тестов»).
  Проверка: приложение поднимается, прежние узлы и записи словарей на месте

## 2. Запись, идентификатор, репозиторий

- [x] 2.1 Завести пакет `ru.locus.problem`, в нём `ProblemId` — record поверх
  `long` с проверкой положительности, по образцу `TaxonomyNodeId`. Проверка:
  модульные тесты на отказ от нуля и отрицательного значения
- [x] 2.2 Завести перечисление `ExamPart` со значениями `FIRST` и `SECOND` —
  имена из `glossary.md`, раздел «Перечисления»; придумывать нельзя. Проверка:
  модульный тест сверяет набор значений
- [x] 2.3 Завести запись `Problem`: идентификатор, подпись (может быть
  пустой), Часть, ключи двух файлов, Темы, Методы, Характеристики. Проверки
  состава — в компактном конструкторе: хотя бы одна Тема, хотя бы один Метод,
  Часть указана, оба ключа файлов не пусты. Проверка: модульные тесты
  на каждый отказ — сценарии «Задача без Темы», «Задача без Метода»,
  «Часть указывается ровно одна»
- [x] 2.4 Реализовать `ProblemRepository` на `JdbcClient`: создание Задачи
  вместе со связями, чтение по идентификатору, чтение Задач узла по номеру,
  замена разметки, замена ключа файла, удаление вместе со связями. Проверка:
  `ProblemRepositoryTest` на каждый метод
- [x] 2.5 Реализовать в том же репозитории выборку Методов, употреблённых
  в Задачах указанной Темы (`problem_topic` → `problem_solution_method` →
  `solution_method`, `DISTINCT`). Проверка: сценарии «Методы Темы», «Тема без
  Задач», «Метод из другой Темы»
- [x] 2.6 **Тест изоляции:** ни один метод `ProblemRepository` не принимает
  `UserId` и вообще идентификатора владельца — Задачи общие, передать
  владельца туда некуда (ADR-0027, `antipatterns.md`, первый пункт). Проверка:
  тест отражением по сигнатурам, по образцу `OwnerIsUnknownToTaxonomyTest`
- [x] 2.7 **Тест изоляции:** чтение Задач возвращает всё, что есть, кем бы
  ни был вошедший. Проверка: сценарий «Задачи, заведённые другим
  Пользователем» — два разных Учителя видят одинаковые списки

## 3. Сервис Задачи и правила разметки

- [x] 3.1 Реализовать `ProblemService`: чтение — без аннотаций прав, заведение,
  правка и удаление — под `@PreAuthorize("hasRole('ADMINISTRATOR')")`, как
  в `TaxonomyService` и `SolutionMethodService`. Проверка: `ProblemAccessTest`
  по образцу `TaxonomyAccessTest` — сценарии «Учитель читает библиотеку»,
  «Учитель пытается изменить библиотеку»
- [x] 3.2 Проверка «Тема — лист»: узел, указанный Задаче, не имеет потомков.
  Внешним ключом это не выражается — отдельной таблицы Тем нет, вид узла
  не хранится (`design.md`, «Схема»). Проверка: **тест на инвариант 1** —
  сценарий «Попытка привязать Задачу к Разделу» (`standards.md`, «Тесты»)
- [x] 3.3 Проверка существования указанных Тем, Методов и Характеристик
  с внятным отказом. Проверка: тесты на каждый вид отсутствующей ссылки
- [x] 3.4 Правка Задачи: разметка, подпись, любой из двух PDF; номер
  не меняется. Проверка: сценарии «Разметка изменена», «Правка не снимает
  обязательности»
- [x] 3.5 Удаление Задачи вместе со связями. Проверка: сценарии «Задача
  удалена», «Разметка соседних Задач не задета»

## 4. Файлы Задачи

- [x] 4.1 Укладка обоих PDF через `FileStorage` — SDK хранилища в сервисе
  не появляется (`standards.md`, «Файлы»). Порядок: положить оба файла,
  затем сохранить запись; при неудаче сохранения — удалить положенное
  (`design.md`, «Порядок укладки файлов»). Проверка: тест на сбой сохранения
  оставляет хранилище без новых файлов
- [x] 4.2 Отдача файлов Задачи только временными подписанными ссылками через
  существующий `FileController`. Постоянного адреса нет ни у одного файла
  (`antipatterns.md`, «Публичный бакет или постоянная ссылка»). Проверка:
  сценарии «Просмотр Задачи», «Ссылка после истечения срока»
- [x] 4.3 Пережатие к PDF не применяется — решает вызывающий, по типу
  содержимого (`standards.md`, «Файлы»). Проверка: тест на побайтовое
  совпадение отданного PDF с положенным
- [x] 4.4 Замена файла: положить новый, переписать ключ, удалить старый —
  именно в этом порядке. Проверка: сценарии «Замена PDF», «Второй файл
  при замене не задет»
- [x] 4.5 Удаление Задачи уносит оба файла из хранилища. Проверка: сценарий
  «Удаление Задачи уносит оба файла»

## 5. Пополнение проверок в дереве и словарях

- [x] 5.1 Объявить в `ru.locus.taxonomy` интерфейс-вопрос «есть ли на узле
  содержимое и какое» и собирать его реализации списком в `TaxonomyService`;
  дописать `refuseUnlessEmpty` (`TaxonomyService.java:157`) так, чтобы отказ
  давал первый непустой ответ. Javadoc пополнить: Задачи теперь **есть**,
  остаются теория и отметки. Проверка: сценарии «Удаление Темы с Задачами»,
  «Удаление Темы, освободившейся от Задач»
- [x] 5.2 Реализовать этот интерфейс в `ru.locus.problem` — «на Теме
  размечены Задачи, столько-то». Зависимость идёт от `problem` к `taxonomy`,
  обратной связи между областями не появляется (`design.md`, «Проверки
  в чужих областях»). Проверка: `mvn test` зелёный, `TaxonomyService`
  не ссылается на пакет `problem` — тест отражением по импортам
- [x] 5.3 Запретить создание потомка у Темы с Задачами: `TaxonomyService`
  отказывает с объяснением, что сначала нужен перенос Задач и он придёт
  с `rubricator-restructure`. Проверка: сценарии «Потомок у Темы с Задачами»,
  «Потомок у Темы без Задач»
- [x] 5.4 Объявить в `ru.locus.dictionary` интерфейс-вопрос «употреблена ли
  запись» и дописать `refuseUnlessUnused` обоих сервисов
  (`SolutionMethodService.java:65`, `CharacteristicService`). Javadoc
  пополнить: разметка Задачи **пришла**, остаются отметки владения. Проверка:
  сценарии «Удаление употреблённого Метода», «Удаление употреблённой
  Характеристики», «Запись, освободившаяся от употребления»
- [x] 5.5 Реализовать этот интерфейс в `ru.locus.problem` по обеим связям —
  `problem_solution_method` и `problem_characteristic`. Проверка:
  `DeletionCheckDebtTest` обновлён: долг по разметке закрыт, долг
  по `mastery-marks` назван по-прежнему

## 6. Долг «правка и удаление только до первого использования»

- [x] 6.1 Объявить в `ru.locus.problem` интерфейс-вопрос «использована ли
  Задача» (реализаций пока нет) и вызывать его из правки и удаления отдельным
  названным методом. Javadoc называет работы, обязанные пополнить условие:
  `assignments` и `submission-review` (ADR-0030). Проверка: правка и удаление
  проходят — реализаций нет, условие истинно тождественно
- [x] 6.2 **Сторожевой тест** `ProblemUsageDebtTest` по образцу
  `DeletionCheckDebtTest`: метод на месте, назван, и его пояснение называет
  обе будущие работы. Пустой метод при уборке кода выглядит лишним
  и удаляется молча — вместе с единственной подсказкой. Проверка: тест
  падает, если из исходника исчезло имя метода или упоминание работ

## 7. Экраны

- [x] 7.1 Расширить правую часть `templates/taxonomy/tree.html`: у выбранной
  Темы — список её Задач (номер, подпись, Методы, Характеристики, Часть)
  и действие «завести Задачу». Проверка: сценарии «Список Задач Темы»,
  «Тема без Задач»
- [x] 7.2 У выбранного Раздела вместо пустого списка — сообщение, что Задачи
  несут только Темы. Пустой список читался бы как «здесь ничего нет» и вёл бы
  к заведению дублей (`design.md`, «Экран»). Проверка: сценарий «Выбран
  Раздел»
- [x] 7.3 Страница заведения и правки Задачи: два файла, подпись, Часть,
  выбор Тем из дерева, выбор Методов и Характеристик. Контроллер тонкий —
  разбор запроса и выбор шаблона, ни логики, ни проверок прав. Проверка:
  `ProblemControllerIsThinTest` по образцу `DictionaryControllerIsThinTest`
- [x] 7.4 В форме разметки Методы Темы показываются первыми, полный словарь —
  отдельным действием (ADR-0009, ADR-0010). Проверка: сценарий «Разметка
  Методом, в Теме не встречавшимся» — выбор не ограничен выборкой
- [x] 7.5 Страница просмотра Задачи: разметка, номер, подпись, обе ссылки
  на файлы. Проверка: сценарий «Просмотр Задачи», текст сообщений сравнивается
  как видимый человеку, а не разметкой целиком (`antipatterns.md`, «Сравнение
  разметки страницы целиком»)
- [x] 7.6 Добавить адрес Задач в `Addresses.java` и пункт в навигацию
  `templates/home.html`, поправив там же фразу о построенном. Проверка:
  переход по ссылке в тесте `Browser`
- [x] 7.7 Тест: Задачи не доступны без входа. Проверка: сценарий «Невошедший
  к Задачам не допускается»

## 8. Проверка целиком

- [x] 8.1 Прогнать `mvn clean package`. Проверка: сборка зелёная, все тесты
  проходят

## 9. Документы проекта

Затрагиваются семь документов; изменение правит каждый.

- [x] 9.1 `openspec/context/glossary.md`: добавить термин **Подпись**
  (`caption`) в раздел «Библиотека» и дополнить строку Задачи составом.
  Проверка: термин на месте, английское имя без транслита (ADR-0028)
- [x] 9.2 `openspec/context/domain-model.md`: в схеме сущностей у Задачи
  назвать подпись и номер; в таблице «Операции, требующие особого поведения»
  отметить, что до `rubricator-restructure` добавление потомков Теме
  с задачами **отклоняется**, а не выполняется с приёмником. Проверка:
  таблица не обещает поведения, которого в системе нет
- [x] 9.3 `openspec/context/scenarios.md`, С1: в шаге заведения Задачи назвать
  необязательную подпись. Проверка: шаг соответствует форме
- [x] 9.4 `openspec/context/standards.md`, раздел «Файлы»: записать порядок
  «сначала в хранилище, потом запись, при неудаче — уборка» и причину —
  Задача без файла выглядела бы настоящей, а забытый файл не виден никому.
  Проверка: правило на месте с причиной, а не как указание
- [x] 9.5 `openspec/context/architecture.md`, вводный абзац: пополнить перечень
  построенного Задачами. Проверка: абзац больше не относит задачи к задуманному
- [x] 9.6 `CLAUDE.md`, раздел «Статус»: назвать шестую возможность
  `problem-catalog` и снять фразу о том, что задач нет. Проверка: раздел
  называет шесть возможностей
- [x] 9.7 Перевести `openspec/context/adr/0029-nomer-i-podpis-zadachi.md`
  и `0030-zadacha-zamorazhivaetsya-posle-vydachi.md` из статуса «Предложено»
  в «Принято» и прогнать `python3 openspec/context/adr/build_index.py`.
  Проверка: `build_index.py --check` молчит, индекс показывает оба статуса

`openspec/context/strategy.md` и `antipatterns.md` не затрагиваются: горизонт
и не-цели изменение не меняет, а все запреты, которых оно касается — задачи
на Разделе, постоянная ссылка на файл, молчаливое удаление, — там уже
записаны, и изменение им следует.
