## 1. Сборка и каркас приложения

- [x] 1.1 Завести Maven-проект: `pom.xml` с `spring-boot-starter-parent` 4.1,
  `java.version` 21, groupId `ru.locus`, artifactId `locus`. Версия образа
  PostgreSQL живёт не в `pom.xml`, а в `compose.yaml`: свойство Maven до Compose
  не дотягивается, а тесты файл прочитать могут (см. `ObrazPostgres`).
  Проверка: `mvn -q validate` проходит.
- [x] 1.2 Подключить стартеры `web`, `thymeleaf`, `jdbc`, `liquibase`;
  `spring-boot-docker-compose` — в области разработки,
  `spring-boot-testcontainers` и драйвер PostgreSQL — в тестовой. Проверка:
  `mvn -q dependency:resolve` проходит, лишних стартеров в дереве нет.
- [x] 1.3 Создать класс `ru.locus.LocusApplication` с `@SpringBootApplication`
  и `application.yaml`. Проверка: `mvn -q compile` проходит.

## 2. База: Compose, Liquibase, Testcontainers

- [x] 2.1 Написать `compose.yaml` в корне: один сервис PostgreSQL нужной версии,
  именованный том, локальные учётные данные открытым текстом. Проверка:
  `docker compose up -d` поднимает базу, `docker compose down` гасит, том
  переживает перезапуск.
- [x] 2.2 Завести master-changelog `db/changelog/db.changelog-master.yaml`
  с `includeAll` из пустой пока папки миграций и включить Liquibase в
  `application.yaml`. Миграции — YAML с абстрактными операциями; сырой SQL
  только там, где иначе нельзя. Проверка: приложение стартует, в базе
  появляются служебные таблицы Liquibase.
- [x] 2.3 Проверить, что `mvn spring-boot:run` поднимает базу через
  `spring-boot-docker-compose` без отдельного шага и приложение подключается
  к ней без ручной настройки параметров.
- [x] 2.4 Поставить `spring.docker.compose.lifecycle-management: start-only`,
  чтобы остановка одного экземпляра не гасила базу под другим. Проверка:
  при двух запущенных экземплярах остановка первого оставляет контейнер базы
  в состоянии `running`.

## 3. Стартовая страница

- [x] 3.1 Добавить контроллер корневого адреса и шаблон Thymeleaf без
  содержания. Проверка: `curl` на корневой адрес запущенного приложения
  возвращает 200 и HTML.

## 4. Тесты

Правил изоляции по владельцу это изменение не вводит: личных данных в нём нет
вовсе (см. `proposal.md`, раздел Impact). Поэтому тестов на фильтр по владельцу
здесь не появляется — они начинаются с `auth-roles`.

- [x] 4.1 Написать базовый класс интеграционного теста: `@SpringBootTest`,
  контейнер PostgreSQL нужной версии через Testcontainers, подключение
  аннотацией `@ServiceConnection`; режим тихого пропуска без Docker
  **не включать**. Проверка: тест-наследник поднимает контекст.
- [x] 4.2 Тест на требование «Схема базы приводится в актуальное состояние
  при старте»: на чистом контейнере контекст поднимается, служебные таблицы
  Liquibase существуют; повторный запуск Liquibase на той же базе ничего
  не применяет заново.
- [x] 4.3 Тест на требование «Несогласованная схема останавливает запуск»:
  при недоступной базе контекст не поднимается, приложение не начинает
  обслуживать запросы.
- [x] 4.4 Тест на требование «Система отдаёт страницы, собранные на сервере»:
  запрос корневого адреса возвращает 200 и тело `text/html`.
- [x] 4.5 Проверка целиком: `mvn test` на чистом клоне репозитория проходит
  с нуля, без ручной подготовки базы.

## 5. Оснастка и документы

- [x] 5.1 `.serena/project.yml`: поставить `language_servers: [java]`; снять
  `.serena/` с игнорирования, оставив в `.gitignore` только `.serena/cache/`
  и локальные файлы. Проверка: `find_symbol` находит `LocusApplication`.
  *Конфигурация проставлена; `.serena/` не игнорировался и так — в `.gitignore`
  только `.serena/cache/`, остальное закрыто вложенным `.serena/.gitignore`.
  Проверка `find_symbol` в текущей сессии не выполнена: serena читает
  `project.yml` при запуске MCP-сервера и отвечает `Active language servers: []`.
  Выполняется первым же обращением к символьным инструментам в новой сессии.*
- [x] 5.2 `CLAUDE.md`, раздел «Команды сборки и тестов»: записать команды
  сборки, запуска, прогона всех тестов, прогона одного теста и сноса базы
  разработки; указать Docker как требование среды. Проверка: каждая команда
  выполнена и работает как написано.
- [x] 5.3a `openspec/context/architecture.md`: в разделе «Развёртывание» —
  порядок обновления с остановкой и подраздел «Локальный запуск»: приложение
  поднимает базу само, но не гасит её, и почему; долгоживущий том как проверка
  апгрейд-пути; тесты идут на своём контейнере. Поправить вступление документа:
  оно утверждает, что прикладного кода нет. В `CLAUDE.md` остаётся только
  операционная часть со ссылкой сюда — причина живёт в одном месте
  ([ADR-0022](../../context/adr/0022-kontekst-kak-ishodniki.md)).
- [x] 5.3 `openspec/context/standards.md`: добавить два правила — о группировке
  кода по предметным областям со слоями внутри и о форме миграций (абстрактные
  операции YAML; сырой SQL как исключение; откаты руками не пишутся, страховка —
  архив базы перед выкаткой; обратная совместимость миграций не требуется,
  обновление идёт с остановкой). Обоснование — `design.md`,
  [ADR-0023](../../context/adr/0023-osnastka-sborki.md),
  [ADR-0024](../../context/adr/0024-obnovlenie-s-ostanovkoj.md).
- [x] 5.4 Перевести в статус «Принято» при архивации изменения записи
  `0023-osnastka-sborki.md` и `0024-obnovlenie-s-ostanovkoj.md`; прогнать
  `python3 openspec/context/adr/build_index.py`. Проверка: `--check` молчит.
- [x] 5.8 По руководству архивации: записать в `openspec/context/antipatterns.md`
  грабли, найденные реализацией (раздел «Оснастка и среда»); сверить
  `strategy.md` — предпосылки П1–П5 и условия возврата отложенного; сверить
  `glossary.md` и `domain-model.md` на новые термины.
- [x] 5.5 `openspec/backlog.md`: отметить `project-skeleton` как выполненный (✅).
- [x] 5.6 Завести элемент бэклога на английские соответствия терминам словаря
  (Open Question из `design.md`) — до `auth-roles`.
- [x] 5.7 `openspec/backlog.md`, карточка `deployment-backup`: записать порядок
  обновления живой системы — остановка, архив базы, накат миграций, подъём —
  и назвать основанием [ADR-0023](../../context/adr/0023-osnastka-sborki.md)
  и [ADR-0024](../../context/adr/0024-obnovlenie-s-ostanovkoj.md). Пропуск
  второго шага лишает выкатку пути назад.

Документы `glossary.md`, `domain-model.md`, `scenarios.md`, `architecture.md`
и `strategy.md` изменение **не затрагивает**: новых терминов, сущностей,
сценариев и архитектурных решений сверх [ADR-0023](../../context/adr/0023-osnastka-sborki.md)
в нём нет, ни одна предпосылка П1–П5 им не проверяется.
