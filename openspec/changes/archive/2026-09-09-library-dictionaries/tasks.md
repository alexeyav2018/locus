## 1. Схема

- [x] 1.1 Завести миграцию `0003-library-dictionaries.yaml`: таблица
  `solution_method` (`id` BIGINT autoincrement, `name` VARCHAR(200) NOT NULL)
  и таблица `characteristic` с тем же составом. Имена выведены из имён классов
  механически (`glossary.md`, «Имена в коде и в базе»), выбирать нечего.
  Проверка: `mvn test -Dtest=MigrationsOnStartupTest` проходит, обе таблицы
  есть в чистой базе
- [x] 1.2 Добавить в ту же миграцию уникальные функциональные индексы
  `unique (lower(name))` по каждой таблице. Без учёта регистра — чтобы
  «Разложение на множители» и «разложение на множители» не разошлись в две
  записи, а с ними и статистика владения (`design.md`, «Уникальность имени»).
  Changeset сырым SQL: абстрактными операциями Liquibase функциональный индекс
  не выражается, отката у него нет — это ожидаемо (`standards.md`, «Данные»).
  Проверка: тесты репозиториев на отказ при повторе имени в другом регистре

## 2. Записи, идентификаторы, репозитории

- [x] 2.1 Завести пакет `ru.locus.dictionary` и в нём `SolutionMethodId`
  и `CharacteristicId` — record поверх `long` с проверкой положительности
  в компактном конструкторе, по образцу `ru.locus.taxonomy.TaxonomyNodeId`.
  Два разных типа, а не один общий: перепутанные местами идентификаторы
  словарей должны быть ошибкой компиляции. Проверка: модульные тесты
  на отказ от нуля и отрицательного значения
- [x] 2.2 Завести записи `SolutionMethod` и `Characteristic` — идентификатор
  и имя; имя не пустое, проверка в компактном конструкторе. Общего
  суперкласса и общего интерфейса не заводить (`design.md`, «Две таблицы
  и две записи»). Проверка: модульный тест на отказ от пустого имени
- [x] 2.3 Реализовать `SolutionMethodRepository` на `JdbcClient` по образцу
  `TaxonomyRepository`: чтение всех записей по алфавиту, чтение по
  идентификатору, поиск по имени без учёта регистра, создание,
  переименование, удаление. Проверка: `SolutionMethodRepositoryTest`
  на каждый метод
- [x] 2.4 Реализовать `CharacteristicRepository` симметрично. Проверка:
  `CharacteristicRepositoryTest` на каждый метод
- [x] 2.5 **Тест изоляции:** ни один метод обоих репозиториев не принимает
  `UserId` и вообще идентификатора владельца — словари общие, передать
  владельца туда некуда (ADR-0027, `antipatterns.md`, первый пункт). Проверка:
  тест отражением по сигнатурам методов, по образцу
  `ru.locus.taxonomy.OwnerIsUnknownToTaxonomyTest`
- [x] 2.6 **Тест изоляции:** ни один метод обоих репозиториев не отдаёт записи
  выборочно — чтение списка возвращает всё, что есть в таблице, кем бы
  ни был вошедший. Фильтр, применённый к библиотеке, сделал бы её невидимой,
  и это ошибка тихая (`domain-model.md`, «Граница общего и личного»).
  Проверка: сценарий «Словарь один для всех» — два разных Учителя видят
  одинаковые списки

## 3. Сервисы и правила словарей

- [x] 3.1 Реализовать `SolutionMethodService`: чтение — без аннотаций прав
  (вход уже потребован рубежом в `SecurityConfig`); заведение,
  переименование и удаление — под `@PreAuthorize("hasRole('ADMINISTRATOR')")`,
  как в `TaxonomyService` (`src/main/java/ru/locus/taxonomy/TaxonomyService.java:109`).
  Проверка: сценарии «Учитель читает словари», «Учитель пытается изменить
  словарь»
- [x] 3.2 Реализовать заведение записи: имя обязательно и не пустое после
  отсечения пробелов по краям, занятость имени проверяется без учёта регистра
  и отклоняется отдельным исключением по образцу
  `ru.locus.taxonomy.NameAlreadyTakenException`
  (`src/main/java/ru/locus/taxonomy/NameAlreadyTakenException.java:11`).
  Проверка: сценарии «Запись заведена», «Пустое имя», «Пробелы по краям имени
  отсекаются», «Одноимённые Методы не сосуществуют»
- [x] 3.3 Реализовать переименование, включая случай сохранения записи под
  её собственным именем — он обязан проходить, а не спотыкаться о проверку
  занятости. Проверка: сценарии «Запись переименована», «Новое имя занято»,
  «Запись переименована в собственное имя»
- [x] 3.4 Реализовать удаление **одним** методом проверки использования —
  отдельным и названным так, чтобы будущее условие дописывалось в одно место,
  а не по всем вызовам удаления. Сегодня условие выполняется тождественно:
  ссылаться на запись нечему, Задач нет. Проверка: сценарии «Неиспользуемая
  запись удалена», «Удаление не задевает второй словарь»
- [x] 3.5 Написать в javadoc этого метода, при появлении чего условие обязано
  пополниться: разметка Задачи (`problem-catalog`) и, следом за ней, отметки
  Владения, чьи ячейки на удаляемый Метод опираются (`mastery-marks`).
  Проверка: комментарий на месте и называет оба элемента бэклога
- [x] 3.6 Реализовать `CharacteristicService` симметрично, с теми же
  правилами имени и удаления. Проверка: сценарии «Одноимённые Характеристики
  не сосуществуют», «Словари независимы»

## 4. Экран

- [x] 4.1 Добавить адрес словарей в `ru.locus.Addresses` и реализовать
  `DictionaryController` — тонкий: разбор запроса и выбор шаблона, ни
  предметной логики, ни проверок прав (`standards.md`, «Слои и границы»).
  Проверка: тест на то, что контроллер не обращается к репозиториям,
  по образцу `TaxonomyControllerIsThinTest`
- [x] 4.2 Шаблон экрана: два списка рядом — Методы и Характеристики, каждый
  по алфавиту, имена ссылками выбора (`?method=<id>`, `?characteristic=<id>`).
  Ни одной внешней библиотеки, ни строки JavaScript; колонки — минимальным
  `<style>` в шаблоне. Проверка: сценарий «Оба словаря видны»;
  `grep -rn "script" src/main/resources/templates` ничего не находит
- [x] 4.3 Формы над выбранной записью: заведение, переименование, удаление.
  Заведение доступно и когда ничего не выбрано. После операции — возврат
  к затронутой записи, она остаётся выбранной. Проверка: сценарии «Действия
  относятся к выбранной записи», «Пока запись не выбрана», «Возврат к записи
  после операции»
- [x] 4.4 Скрыть формы от Пользователя без роли Администратора — условием
  «показать или нет», а не вычислением в шаблоне (`antipatterns.md`, «Логика
  в шаблонах Thymeleaf»). Пояснения разработчику — комментарием Thymeleaf,
  не HTML-комментарием: обычный уходит в страницу целиком, вместе с названиями
  действий, которых Учителю не предлагают (`antipatterns.md`). Проверка:
  сценарий «Учителю операций не предлагают»
- [x] 4.5 Поведение при записи, которой больше нет: словари показываются,
  действий не предлагается, об исчезнувшей сообщается. Проверка: сценарий
  «Выбрана запись, которой больше нет»
- [x] 4.6 Добавить пункт словарей в навигацию `templates/home.html`
  и поправить там же фразу о построенном. Проверка: со страницы после входа
  на словари можно перейти ссылкой, тест переходит по ней `Browser`
- [x] 4.7 Тест: словари не доступны без входа. Проверка: сценарий
  «Невошедший к словарям не допускается»

## 5. Проверка целиком

- [x] 5.1 Прогнать `mvn clean package`. Проверка: сборка зелёная, все тесты
  проходят

## 6. Документы проекта и бэклог

Затрагиваются три документа; изменение правит каждый.

- [x] 6.1 `openspec/backlog.md`, карточка `problem-catalog`: перенести туда
  из карточки `library-dictionaries` пункт «выборка методов, уже встречавшихся
  в заданной Теме» вместе с соответствующей частью признака готовности —
  это запрос по разметке, а не по справочнику (`proposal.md`, «Что не входит»).
  Проверка: пункт стоит ровно в одной карточке, и в той, где для него есть
  данные
- [x] 6.2 `openspec/backlog.md`, карточка `problem-catalog`: добавить пункт
  «пополнить проверку удаления записи словаря — удалять только неупотреблённую
  в разметке». Долг записан требованием спеки, но найти его должен тот, кто
  возьмёт `problem-catalog`, а он читает карточку. Проверка: пункт на месте
  и называет обе таблицы связи
- [x] 6.3 `openspec/context/architecture.md`, вводный абзац: пополнить
  перечень построенного словарями. Проверка: абзац больше не относит словари
  к задуманному
- [x] 6.4 `CLAUDE.md`, раздел «Статус»: пополнить перечень возможностей
  `library-dictionaries` и назвать построенное. Проверка: раздел называет
  пять возможностей вместо четырёх

Записи в журнале решений изменение **не заводит**: новых решений оно
не принимает, обоснование — в `proposal.md`. `glossary.md`, `domain-model.md`,
`scenarios.md`, `strategy.md`, `standards.md` и `antipatterns.md`
не затрагиваются: термины Метода и Характеристики, их роль в разметке
и запрет дробления Метода по Темам там уже записаны, и изменение им следует,
а не меняет их.
