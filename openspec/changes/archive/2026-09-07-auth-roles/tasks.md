## 1. Оснастка и схема

- [x] 1.1 Добавить в `pom.xml` зависимости `spring-boot-starter-security`
      и `spring-security-test`; убедиться, что `mvn -q compile` проходит,
      а версия Spring Security — 7.1.1 (`mvn help:evaluate
      -Dexpression=spring-security.version -DforceStdout`)
- [x] 1.2 Написать миграцию `src/main/resources/db/changelog/migrations/`:
      таблицы `user_` (идентификатор, имя входа с уникальным индексом, хеш
      пароля, признак «пароль подлежит смене») и `user_role` (внешний ключ
      `user_id`, значение роли) абстрактными операциями Liquibase; проверить
      `mvn test -Dtest=MigraciiPriStarteTest` — миграция применяется на чистом
      контейнере и не применяется повторно
- [x] 1.3 Тем же changelog'ом вставить учётную запись `admin` с ролью
      `ADMINISTRATOR`, хешем начального пароля в формате `{алгоритм}хеш`
      и поднятым признаком смены; проверить тестом, что после миграций
      на пустой базе такая запись ровно одна и признак поднят
      (сценарии «Первый запуск на пустой базе», «Повторный запуск»)

## 2. Пользователи и роли

- [x] 2.1 Завести пакет `ru.locus.user`: `Role` (перечисление `ADMINISTRATOR`,
      `TEACHER` по [glossary.md](../../context/glossary.md)), `UserId`
      (отдельный тип идентификатора), `User`; убедиться, что имена совпадают
      со столбцом «В коде» словаря
- [x] 2.2 Реализовать `UserRepository` на `JdbcClient`: поиск по имени входа,
      список записей, создание, смена хеша и признака, назначение и снятие
      роли; проверить интеграционным тестом на контейнере, что созданная
      запись читается обратно вместе с ролями
- [x] 2.3 Реализовать `UserService` с проверками прав `@PreAuthorize` на
      операциях Администратора и `CurrentUser` — единственный компонент,
      знающий о вошедшем; проверить, что `CurrentUser` отдаёт `UserId`
      вошедшего и бросает осмысленную ошибку, когда входа нет

## 3. Вход, роли и смена пароля

- [x] 3.1 Завести пакет `ru.locus.security`: `SecurityConfig`
      с bean'ами `SecurityFilterChain` и `PasswordEncoder`
      (`PasswordEncoderFactories.createDelegatingPasswordEncoder()`),
      `@EnableMethodSecurity`; адаптер `UserDetailsService` поверх
      `UserRepository`, добавляющий префикс `ROLE_` к значению роли.
      Проверить: вход верной парой проходит, неверной — нет
- [x] 3.2 Сделать `LoginController` и шаблон формы входа на Thymeleaf; проверить
      сценарии «Верные имя входа и пароль», «Неверный пароль», «Несуществующее
      имя входа» — в двух последних сообщение об отказе одинаковое
- [x] 3.3 Закрыть все адреса, кроме формы входа и статики; проверить сценарии
      «Обращение к странице без входа» и «Обращение к адресу, на который нет
      ссылки» — оба приводят к форме входа
- [x] 3.4 Сделать выход; проверить сценарий «Выход» — после него обращение
      к любой странице приводит к форме входа
- [x] 3.5 Реализовать `PasswordChangeFilter` перед `AuthorizationFilter`: при
      поднятом признаке пропускаются только форма смены пароля, её отправка,
      выход и статика. Проверить сценарии «Первый вход с назначенным паролем»,
      «Попытка обойти смену пароля» (обращение к странице Администратора
      напрямую), «Пароль сменён»
- [x] 3.6 Сделать смену собственного пароля с подтверждением текущего;
      проверить сценарии «Смена пароля» и «Текущий пароль указан неверно»

## 4. Ведение учётных записей

- [x] 4.1 Сделать `UserController` и шаблоны: список записей, заведение новой,
      назначение и снятие ролей, сброс пароля; проверить сценарии «Заведение
      пользователя», «Имя входа занято», «Назначение и снятие роли»,
      «Сброс пароля»
- [x] 4.2 Проверить тестами разграничение по ролям: сценарии «Учитель
      обращается к операции Администратора», «Учитель обращается к учётным
      записям», «Пользователь совмещает роли», «Пользователь без ролей».
      Отказ проверяется обращением напрямую по адресу, а не отсутствием
      ссылки в разметке
- [x] 4.3 Проверить, что заведённый Администратором пользователь входит
      назначенным паролем и сразу попадает на форму смены

## 5. Уборка каркаса

- [x] 5.1 Удалить пакет `ru.locus.startovaya` и шаблон `startovaya.html`;
      сделать главную страницу вошедшего; убедиться, что `mvn -q compile`
      проходит и висящих ссылок не осталось
- [x] 5.2 Переписать `StartovayaStranicaTest` под изменённое требование
      спецификации `application-startup`: сценарий «Обращение к корневому
      адресу без входа» (приводит к форме входа, 200 HTML, разметка собрана
      на сервере) и «Обращение к корневому адресу» (для вошедшего)
- [x] 5.3 Переименовать оставшиеся транслитные имена каркаса по
      [ADR-0028](../../context/adr/0028-imena-v-kode-tolko-anglijskie.md):
      `IntegracionnyjTest`, `MigraciiPriStarteTest`, `NesoglasovannayaShemaTest`,
      `ObrazPostgres` — вместе с их методами, полями и константами
      (`izComposeFile`, `naPustojBazeMigraciiNakatyvayutsya`,
      `NEDOSTUPNAYA_BAZA` и прочими). Переименовывать `rename_symbol` (serena),
      а не текстовой заменой; javadoc и комментарии остаются русскими.
      Проверить `mvn test` — состав проверок не изменился, изменились имена
- [x] 5.4 Прогнать `mvn test` целиком: все тесты зелёные, включая
      переименованный тест несогласованной схемы, содержание которого трогать
      не требуется

## 6. Документы проекта

- [x] 6.1 Дописать в [standards.md](../../context/standards.md) два правила:
      [ADR-0027](../../context/adr/0027-mehanizm-granicy-obshchego-i-lichnogo.md)
      (владелец — обязательный параметр репозитория личного контура,
      библиотечный такого параметра не принимает) и
      [ADR-0028](../../context/adr/0028-imena-v-kode-tolko-anglijskie.md)
      в разделе «Язык» (все идентификаторы английские, транслита нет; русский —
      комментарии, интерфейс, сообщения, артефакты OpenSpec). Убедиться, что
      прежняя формулировка раздела «Язык» не противоречит новой
- [x] 6.2 Дописать в [antipatterns.md](../../context/antipatterns.md) грабли,
      на которые наступили при реализации; если не наступили ни на одни —
      сказать об этом явно и ничего не дописывать
- [x] 6.3 Перенести признак готовности «учитель А не получает учеников
      учителя Б» из карточки `auth-roles` в карточку `students-groups`
      (`openspec/backlog.md`), заменив его в `auth-roles` на проверяемое
      здесь; дописать в `students-groups`, что вместе с первым репозиторием
      личного контура правило
      [ADR-0027](../../context/adr/0027-mehanizm-granicy-obshchego-i-lichnogo.md)
      становится принудительным (сигнатуры плюс тест), — иначе знание
      потеряется между двумя изменениями; отметить `auth-roles` закрытым
- [x] 6.4 Обновить `CLAUDE.md`: сводка ключевых решений пополняется
      [ADR-0026](../../context/adr/0026-vhod-i-pervyj-administrator.md)
      и [ADR-0027](../../context/adr/0027-mehanizm-granicy-obshchego-i-lichnogo.md);
      назвать имя входа и начальный пароль первого Администратора
      и совет завести вторую административную запись
- [x] 6.5 Перевести ADR-0026, ADR-0027 и ADR-0028 из «Предложено»
      в «Принято», прогнать `python3 openspec/context/adr/build_index.py`
      и убедиться, что `--check` не ругается
- [x] 6.6 Проверить, что переименование спеки `zapusk-prilozheniya` →
      `application-startup` доведено до конца: ссылок на старое имя не осталось
      нигде, кроме `openspec/changes/archive/` и ADR-0028, где оно —
      исторический факт и правке не подлежит
