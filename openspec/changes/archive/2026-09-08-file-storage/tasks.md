## 1. Зависимости и настройки

- [x] 1.1 Добавить в `pom.xml` AWS SDK v2 (`s3`), Thumbnailator и тестовый
  `testcontainers-minio`; версии SDK взять из спецификации зависимостей Spring
  Boot, где она ими управляет, а не проставлять руками. Проверка:
  `mvn -q dependency:tree` показывает все три, `mvn clean package` собирается
- [x] 1.2 Завести настройки в `src/main/resources/application.yaml`:
  `locus.file.storage` (`local` по умолчанию), `locus.file.link-ttl`,
  `locus.file.local.directory`, `locus.file.local.secret`,
  `locus.file.object.{endpoint,region,bucket,access-key,secret-key}`,
  `locus.file.image.{max-side,quality}`. Проверка: приложение поднимается
  `mvn spring-boot:run` без единого заданного снаружи значения
- [x] 1.3 Добавить каталог локального хранилища в `.gitignore`. Проверка:
  `git status` после укладки файла в разработке чист

## 2. Интерфейс и ключ

- [x] 2.1 Завести пакет `ru.locus.file` и в нём `FileKey` — record поверх
  строки, по образцу `ru.locus.user.UserId` (отдельный тип, проверка значения
  в компактном конструкторе). Проверка: модульный тест на отказ от пустого
  и от значения с разделителем пути
- [x] 2.2 Завести интерфейс `FileStorage` с тремя операциями: положить
  (содержимое и тип), выдать временную ссылку, удалить. Метода чтения
  по ключу нет — решение и его причина в `design.md`. Проверка: у интерфейса
  ровно три метода, ни один не принимает `UserId`
- [x] 2.3 Завести генерацию ключа: UUID плюс расширение, выведенное из типа
  содержимого. Проверка: тест на то, что два вызова с одинаковым типом дают
  разные ключи, а расширение соответствует типу

## 3. Файловая реализация и адрес отдачи

- [x] 3.1 Реализовать `FileStorage` на локальной папке: укладка по ключу,
  удаление, удаление отсутствующего — не ошибка. Проверка: сценарии
  «Файл положен и прочитан», «Повторное удаление» из спеки проходят
- [x] 3.2 Реализовать подпись ссылки: HMAC-SHA256 над парой «ключ + срок»
  на секрете из настройки, при пустой настройке — секрет, сгенерированный
  при старте. Проверка: модульный тест на то, что подпись меняется при смене
  ключа, срока и секрета
- [x] 3.3 Добавить адрес отдачи в `ru.locus.Addresses` и контроллер, который
  его обслуживает: проверяет подпись и срок, отдаёт содержимое с типом,
  выведенным из расширения ключа. Проверка: сценарии «Действующая ссылка»,
  «Истёкшая ссылка», «Обращение без подписи», «Подделанная подпись»
- [x] 3.4 Открыть этот адрес в `SecurityConfig` наравне с формой входа
  и статикой — пропуск даёт подпись, а не сеанс. Проверка: тест на то, что
  ссылка открывается без входа, а тот же адрес без подписи — не открывается

## 4. Объектная реализация

- [x] 4.1 Реализовать `FileStorage` поверх AWS SDK v2: укладка объекта,
  предподписанная ссылка на чтение, удаление. SDK не должен появиться нигде,
  кроме этого класса и его конфигурации. Проверка:
  `grep -rn "software.amazon.awssdk" src/main/java` находит только пакет
  `ru.locus.file`
- [x] 4.2 Развести бины двух реализаций по настройке `locus.file.storage`,
  умолчание — файловая. Проверка: тест поднимает контекст с каждым значением
  и убеждается, что внедряется ожидаемая реализация и ровно одна

## 5. Пережатие изображений

- [x] 5.1 Реализовать `ImageCompression` — отдельный класс, не часть
  хранилища: уменьшение до предела по большей стороне, JPEG с качеством
  из настройки, разворот по сведениям о съёмке. Проверка: сценарии
  «Крупный снимок» и «Снимок, сделанный повёрнутой камерой»
- [x] 5.2 Проверить, что компонент не применяется к PDF: PDF доходит
  до хранилища байт в байт. Проверка: сценарий «PDF» из спеки, сравнение
  содержимого до и после
- [x] 5.3 Подготовить тестовые изображения — крупное и снятое повёрнутой
  камерой — и положить их в тестовые ресурсы. Проверка: файлы читаются
  из classpath, у второго в EXIF стоит ориентация, отличная от обычной

## 6. Тесты контракта

- [x] 6.1 Завести `MinioImage` рядом с `PostgresImage`: версия образа MinIO
  задаётся здесь и больше нигде. Проверка: `grep -rn "minio" --include=*.java
  --include=*.yaml .` находит версию в одном месте
- [x] 6.2 Написать абстрактный `FileStorageContractTest` со всеми сценариями
  спеки: укладка и чтение по ссылке, разные ключи при одинаковом имени,
  несуществующий ключ, четыре сценария подписи, удаление и ссылка на удалённый
  файл, повторное удаление. Чтение — только по выданной ссылке, обращением
  по HTTP. Проверка: класс не содержит ни одного упоминания конкретной
  реализации
- [x] 6.3 Подставить в контракт файловую реализацию. Проверка:
  `mvn test -Dtest=LocalFileStorageTest` зелёный
- [x] 6.4 Подставить объектную реализацию против MinIO, поднятого
  Testcontainers, по образцу `IntegrationTest` — без режима «нет Docker —
  пропустить». Проверка: `mvn test -Dtest=ObjectFileStorageTest` зелёный,
  а при остановленном Docker падает, а не пропускается
- [x] 6.5 Написать тест на недоступное хранилище: операция завершается явной
  ошибкой, отличимой от «файла нет», причина попадает в журнал, а страницы,
  не касающиеся файлов, продолжают открываться. Проверка: сценарии требования
  «Недоступность хранилища не останавливает систему»

## 7. Правила изоляции

- [x] 7.1 Тестов на фильтр по владельцу здесь **нет, и это произнесено
  сознательно**: изменение не заводит ни личных данных, ни репозиториев,
  а хранилище владельца не знает. Вместо них — тест на то, что ни один метод
  `FileStorage` и ни одна из реализаций не принимает `UserId`. Проверка: тест
  падает, если такой параметр появится

## 8. Документы проекта

- [x] 8.1 Поправить `openspec/context/architecture.md`: преамбула утверждает,
  что сказанное о файлах «задумано, но не написано», — после этого изменения
  это неверно. Проверка: преамбула перечисляет как построенное то, что
  действительно построено
- [x] 8.2 Дописать `openspec/context/standards.md`, раздел «Файлы»: ключ
  выдаёт хранилище (осмысленный ключ угадывается), пережатие живёт вне
  `FileStorage`, локальная реализация подписывает ссылку так же, как объектная.
  Проверка: каждое правило названо с причиной, как соседние
- [x] 8.3 Поправить в `CLAUDE.md` раздел «Статус»: возможностей становится
  три. Проверка: перечень совпадает с `openspec/specs/`
- [x] 8.4 Остальные документы контекста изменение **не затрагивает**, и это
  проверено: `glossary.md` — новых терминов нет, файл предметной сущностью
  не является; `domain-model.md` — сущностей и связей не прибавилось;
  `scenarios.md` — сценарии учителя не меняются; `antipatterns.md` — новых
  граблей не появилось, существующие проверены таблицей в `design.md`;
  журнал решений — новых записей нет, изменение строит по
  [ADR-0021](../../context/adr/0021-hranilishche-fajlov.md). Проверка:
  `git status` не показывает эти файлы изменёнными

## 9. Проверка целиком

- [x] 9.1 Прогнать `mvn clean package` и убедиться, что все тесты зелёные,
  включая оба наследника контракта
- [x] 9.2 Поднять приложение `mvn spring-boot:run`, положить файл через
  временный тестовый вызов и открыть выданную ссылку в браузере; убедиться,
  что она перестаёт работать по истечении срока
- [x] 9.3 Прогнать `npx -y @fission-ai/openspec@latest validate --all`
  и убедиться, что изменение и спеки проходят проверку
