**Затрагиваемые документы проекта** (правило `rules.tasks`):
`openspec/context/strategy.md` — переписывается;
`openspec/context/strategy-archive/01-strategy.md` — создаётся;
`openspec/context/adr/0022-*.md` и `adr/README.md` — новая запись и индекс;
`CLAUDE.md` — новый раздел.
Не затрагиваются: `glossary.md`, `domain-model.md`, `scenarios.md`,
`architecture.md`, `standards.md`, `antipatterns.md`, `openspec/backlog.md`.

## 1. Архив прежней редакции

- [x] 1.1 Создать каталог `openspec/context/strategy-archive/` и перенести в него текущий текст стратегии под именем `01-strategy.md`; проверить, что содержимое совпадает с прежним `strategy.md` побайтно (`diff` не выдаёт различий до перезаписи оригинала)

## 2. Редакция 1 стратегии

- [x] 2.1 Написать шапку `strategy.md`: номер редакции, дата, горизонт (закрыт элемент бэклога `mastery-views` — сквозной проход замкнут) и предпосылки П1–П5; проверить, что горизонт сформулирован как проверяемое условие, а не как намерение
- [x] 2.2 Пометить каждый раздел документа его потребителем (разработчик, агент, учитель); проверить, что разделы, помеченные «учитель», не содержат ссылок на бэклог, журнал и стек
- [x] 2.3 Переписать раздел этапов как вехи ценности — что учитель получает на каждой; проверить, что ни одна веха не названа идентификатором бэклога
- [x] 2.4 Оставить не-цели индексом к записям журнала: формулировка в одну строку плюс ссылка; проверить, что все ссылки ведут на существующие файлы
- [x] 2.5 Дополнить таблицу отложенного колонкой состояния триггера со значением «не сработало» по всем строкам; проверить, что у каждой строки условие возврата сформулировано проверяемо
- [x] 2.6 Заменить раздел «Что меняет стратегию» на «Условия истечения» — горизонт и предпосылки с указанием, что делать при срабатывании каждого; проверить, что описан переход к следующей редакции через архив и запись журнала

## 3. Запись журнала

- [x] 3.1 Создать `openspec/context/adr/0022-kontekst-kak-ishodniki.md` со статусом `Предложено`, `area: процесс`, изложив решение о версионировании стратегии, границе «смысл против формулировки» и правке `config.yaml` как оснастки; в отвергнутых альтернативах перечислить снимок, живой документ и вариант B (отдельное предварительное изменение)
- [x] 3.2 Прогнать `python3 openspec/context/adr/build_index.py` и убедиться, что индекс пересобрался без ошибок и запись 0022 появилась в таблице

## 4. CLAUDE.md

- [x] 4.1 Добавить раздел «Документы проекта — исходники»: `openspec/context/` и сам `CLAUDE.md` правятся через цикл изменений, с границей «смысл против формулировки» и ссылкой на ADR-0022; проверить, что раздел не пересказывает стратегию, а отсылает к ней

## 5. Проверка

- [x] 5.1 Прогнать проверку перекрёстных ссылок по всем документам `openspec/context/`, `openspec/backlog.md` и `CLAUDE.md` — битых ссылок ноль
- [x] 5.2 Прогнать `python3 openspec/context/adr/build_index.py --check` и `openspec validate --all` — обе команды завершаются без ошибок
- [x] 5.3 Прочитать `strategy.md` глазами учителя: убедиться, что документ отвечает на вопросы «что я получу», «чего не будет и почему», «когда мы поймём, что план изменился», не требуя открывать другие файлы
