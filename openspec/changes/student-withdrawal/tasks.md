# Tasks

## 1. Схема и модель

- [ ] 1.1 Миграция `0010-student-withdrawal.yaml`: `addColumn withdrawn
      boolean not null default false` на `student`; проверить —
      `mvn spring-boot:run` поднимает базу и применяет миграцию без ошибок
      на уже заполненной базе разработки
- [ ] 1.2 Добавить поле `withdrawn` в `Student` (`Student.java:23`);
      проверить — компиляция и существующие тесты `StudentRepositoryTest`
      не ломаются после правки конструктора (найти все вызовы через
      `find_referencing_symbols` перед правкой сигнатуры)
- [ ] 1.3 `StudentRepository`: `findAll(UserId owner, boolean withdrawn)`
      вместо `findAll(UserId owner)`, `withdraw`/`restore`
      (`update student set withdrawn = ? where user_id = ? and id = ?`);
      проверить тестом с двумя владельцами и обоими значениями флага
      (форма — как у существующих методов, `OwnerIsRequiredByStudentsTest`
      не должен потерять эти методы из списка)

## 2. Ведение выбытия и список

- [ ] 2.1 `StudentService`: методы `withdraw(StudentId)` /
      `restore(StudentId)` с проверкой владельца (как `rename`), `all()`
      принимает флаг «включая выбывших» или разделяется на два вызова
      по решению design.md («Фильтрация — на уровне репозитория»);
      проверить тестом, что чужого Ученика пометить нельзя
      (`StudentNotFoundException`)
- [ ] 2.2 `StudentController`: `/students` принимает параметр показа
      выбывших, маршруты `POST /students/{id}/withdrawal` и
      `POST /students/{id}/withdrawal/undo` (или одна операция
      с переключением — на усмотрение реализации, по образцу
      `POST .../deletion`); проверить интеграционным тестом обе операции
      и то, что выбывший пропадает из списка по умолчанию
- [ ] 2.3 Шаблон `student/list.html`: переключатель «показать выбывших»,
      пометка выбывшего в перечне; `student/student.html`: кнопка
      «Пометить выбывшим» / «Вернуть» в зависимости от состояния;
      проверить вручную через `mvn spring-boot:run`

## 3. Прямая выдача одному Ученику

- [ ] 3.1 `AssignmentController.renderForm`: список Учеников для формы
      берёт только действующих; проверить — форма `/assignments/new`
      не предлагает выбывшего
- [ ] 3.2 `AssignmentService.issueToStudent`: отклонять выдачу выбывшему
      Ученику с понятным сообщением, независимо от формы; проверить
      тестом прямой вызов сервиса с идентификатором выбывшего Ученика
- [ ] 3.3 Тест: Ученик, выбывший после выдачи, не теряет ранее выданное
      Задание (`assignments` списка Ученика не меняется от выбытия)

## 4. Выдача Группе

- [ ] 4.1 `AssignmentService.issueToGroup`: отфильтровать выбывших членов
      Группы до проверки на пустоту и до создания Заданий; отдельное
      сообщение об отказе, когда действующих не осталось; проверить
      тестами: Группа из пяти с двумя выбывшими даёт три Задания;
      Группа, где выбыли все, отклоняется с сообщением про отсутствие
      действующих Учеников
- [ ] 4.2 Проверить сценарий «Группа после выдачи переименована
      и удалена» (`assignments`, уже существующий) остаётся зелёным —
      выбытие члена после выдачи не должно трогать созданные Задания
      и Раздачу

## 5. Документы

- [ ] 5.1 Добавить в `openspec/context/glossary.md` термин «выбытие» /
      «выбыл» рядом со строкой «Ученик» в блоке «Личный контур»
- [ ] 5.2 Свериться с антипаттернами (`openspec/context/antipatterns.md`)
      и стандартами (`openspec/context/standards.md`) по слоям —
      подтвердить, что контроллеры остались тонкими, а границу
      общего/личного не задели; проверить — `mvn clean package` зелёный
