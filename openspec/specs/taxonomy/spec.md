## Purpose

Рубрикатор — дерево классификации задач произвольной глубины, общее на всю
систему. Описывает состав узла, различие Раздела и Темы, ведение дерева
Администратором и обход поддерева, на котором потом держится правило «поиск
по узлу включает всё поддерево». Прикладного смысла для учителя здесь пока
нет: это ось, вдоль которой раскладываются Задачи и измеряется Владение.

## Requirements

### Requirement: Рубрикатор — дерево с единственным родителем у узла

Система SHALL хранить рубрикатор как дерево произвольной глубины. Узел имеет
имя и принадлежит **ровно одному** родителю либо не имеет родителя вовсе —
тогда он корневой. Корневых узлов может быть несколько.

Глубина ничем не ограничена: заранее заданный предел упёрся бы в учителя
ровно в тот момент, когда тот уточняет структуру, то есть делает правильную
вещь ([ADR-0006](../../context/adr/0006-rubrikator-derevo.md)).

#### Scenario: Дерево строится на четыре уровня

- **WHEN** создан корневой узел, у него потомок, у потомка свой потомок,
  а у того — ещё один
- **THEN** все четыре узла существуют
- **AND** у каждого некорневого узла родителем значится тот, под которым
  он создан

#### Scenario: Несколько корней

- **WHEN** создано два узла без родителя
- **THEN** оба существуют как корневые
- **AND** дерево показывает обоих

### Requirement: Вид узла определяется наличием потомков, а не хранится

Система SHALL считать узел **Темой**, если у него нет потомков, и **Разделом**,
если потомки есть. Отдельного хранимого признака вида у узла нет.

Вид узла — вычисляемое свойство. Хранимый признак был бы вторым источником
правды о том, лист ли узел, и разошёлся бы с первым молча — в тот момент,
когда у Темы появляется потомок. Расхождение здесь стоит дорого: на «узел —
лист» опираются привязка Задачи (инвариант 1) и отметка Владения
(инвариант 3) ([domain-model.md](../../context/domain-model.md)).

#### Scenario: Новый узел — Тема

- **WHEN** создан узел, у которого нет потомков
- **THEN** его вид — Тема

#### Scenario: Тема становится Разделом при появлении потомка

- **WHEN** узлу, бывшему Темой, создан потомок
- **THEN** вид этого узла — Раздел
- **AND** вид созданного потомка — Тема

#### Scenario: Раздел становится Темой, когда последний потомок ушёл

- **WHEN** у Раздела с единственным потомком этот потомок перемещён
  под другого родителя
- **THEN** вид прежнего родителя — Тема

### Requirement: Ведёт дерево только Администратор, читают все вошедшие

Система SHALL допускать создание, переименование, перемещение и удаление узла
только Пользователю с ролью Администратора. Просмотр дерева доступен любому
вошедшему Пользователю независимо от ролей.

Рубрикатор — часть общей библиотеки. Он **не фильтруется по владельцу**:
у операций чтения дерева нет и не может быть параметра «чей»
([ADR-0004](../../context/adr/0004-mnogouchitelskaya-rabota.md),
[ADR-0027](../../context/adr/0027-mehanizm-granicy-obshchego-i-lichnogo.md)).
Все Учителя видят одно и то же дерево — то, которое ведёт Администратор.

#### Scenario: Учитель читает дерево

- **WHEN** вошедший Пользователь с ролью Учителя открывает рубрикатор
- **THEN** дерево показывается целиком

#### Scenario: Учитель пытается изменить дерево

- **WHEN** Пользователь без роли Администратора пытается создать,
  переименовать, переместить или удалить узел
- **THEN** операция отклоняется
- **AND** дерево остаётся прежним

#### Scenario: Два учителя видят одно дерево

- **WHEN** дерево открывают два разных Учителя
- **THEN** каждый видит все узлы, независимо от того, кто их создавал

#### Scenario: Невошедший к дереву не допускается

- **WHEN** дерево запрашивается без входа в систему
- **THEN** содержимое не показывается

### Requirement: Создание узла

Система SHALL позволять Администратору создать узел, указав имя и, при
наличии, родителя. Узел без указанного родителя становится корневым.

Имя узла обязательно, не пустое после отсечения пробелов по краям
и **уникально среди узлов одного родителя**. Уникальность именно среди
братьев, а не по всему дереву: «признаки подобия» — законное имя и под
треугольниками, и под окружностями, а два одинаковых имени в одном списке
неразличимы для того, кто выбирает Тему для задачи.

Создание потомка у Темы, на которой висят Задачи, MUST отклоняться. Тема
с появлением потомка становится Разделом, а на Разделе Задач не бывает
никогда (инвариант 1,
[domain-model.md](../../context/domain-model.md)): её Задачи оказались
бы висящими на узле, который их нести не может, — в дереве они больше
не находятся, в статистике не участвуют, и никакой ошибки при этом
не выдано.

Отказ временный и назван прямо: перенос Задач на **Тему-приёмник** при
углублении дерева ([ADR-0007](../../context/adr/0007-pravka-rubrikatora.md))
целиком принадлежит элементу бэклога `rubricator-restructure` и здесь
не строится. До него Тему с Задачами углубить нельзя, и Администратору
сообщается почему.

#### Scenario: Узел создан

- **WHEN** Администратор создаёт узел с непустым именем под указанным
  родителем
- **THEN** узел появляется среди потомков этого родителя

#### Scenario: Имя повторяется среди братьев

- **WHEN** Администратор создаёт узел с именем, которое уже носит другой
  потомок того же родителя
- **THEN** узел не создаётся
- **AND** сообщается, что имя занято

#### Scenario: То же имя под другим родителем

- **WHEN** Администратор создаёт узел с именем, которое уже встречается
  в дереве, но под другим родителем
- **THEN** узел создаётся

#### Scenario: Пустое имя

- **WHEN** Администратор создаёт узел с пустым именем или именем из одних
  пробелов
- **THEN** узел не создаётся

#### Scenario: Потомок у Темы с Задачами

- **WHEN** Администратор создаёт потомка у Темы, на которой размечена хотя бы
  одна Задача
- **THEN** узел не создаётся
- **AND** сообщается, что сначала нужно перенести Задачи, а перенос появится
  вместе с механикой Темы-приёмника
- **AND** Задачи Темы остаются на месте

#### Scenario: Потомок у Темы без Задач

- **WHEN** Администратор создаёт потомка у Темы, на которой Задач нет
- **THEN** узел создаётся
- **AND** вид прежней Темы — Раздел

### Requirement: Переименование узла

Система SHALL позволять Администратору изменить имя любого узла. Ограничений
на переименование, кроме требований к самому имени, нет: положение узла
в дереве, его потомки и его идентификатор не меняются
([ADR-0007](../../context/adr/0007-pravka-rubrikatora.md)).

#### Scenario: Узел переименован

- **WHEN** Администратор задаёт узлу новое имя
- **THEN** узел показывается под новым именем
- **AND** его родитель и потомки остаются прежними

#### Scenario: Новое имя занято братом

- **WHEN** Администратор задаёт узлу имя, которое уже носит другой потомок
  того же родителя
- **THEN** имя не меняется

### Requirement: Перемещение узла

Система SHALL позволять Администратору перенести узел под другого родителя
или в корень. Узел переезжает **вместе со всем своим поддеревом**: взаимное
расположение потомков не меняется.

Перемещение узла внутрь собственного поддерева MUST быть запрещено — в том
числе перемещение узла под самого себя. Такая операция отрезала бы ветку
от корня: узлы продолжили бы существовать, но перестали бы находиться
обходом от корня, и потеря была бы тихой.

#### Scenario: Узел перенесён с поддеревом

- **WHEN** Администратор переносит узел с потомками под другого родителя
- **THEN** узел значится среди потомков нового родителя
- **AND** его собственные потомки остаются при нём
- **AND** прежний родитель этого узла среди потомков больше не содержит

#### Scenario: Узел поднят в корень

- **WHEN** Администратор переносит узел, указав отсутствие родителя
- **THEN** узел становится корневым

#### Scenario: Перенос внутрь своего поддерева

- **WHEN** Администратор пытается перенести узел под один из его собственных
  потомков — прямой или отдалённый
- **THEN** перенос отклоняется
- **AND** дерево остаётся прежним

#### Scenario: Перенос узла под самого себя

- **WHEN** Администратор пытается назначить узлу родителем его самого
- **THEN** перенос отклоняется

#### Scenario: Имя занято у нового родителя

- **WHEN** Администратор переносит узел под родителя, среди потомков которого
  уже есть узел с таким именем
- **THEN** перенос отклоняется

### Requirement: Удаляется только пустой узел

Система SHALL позволять Администратору удалить узел **только если узел пуст**.
Пустой — значит не имеющий ни потомков, ни собственного содержимого. Удаление
поддерева одним действием не предусмотрено: узлы снимаются по одному, снизу
вверх, и каждый шаг виден.

Содержимым узла считается всё, что на нём висит: Задачи и отметки Владения —
на Теме, Теоретические материалы — на любом узле. Задачи с появлением
разметки существуют, и проверка отсутствия Задач на удаляемой Теме
**действует**. Отметок Владения и Теоретических материалов в системе
по-прежнему нет, поэтому эти две части проверки остаются тождественными,
и **появление каждой из них обязано пополнять её дальше**: узел, удалённый
вместе с накопленными суждениями учителей об учениках, — невосполнимая
и тихая потеря
([ADR-0007](../../context/adr/0007-pravka-rubrikatora.md)).

Удаление узла с содержимым — отдельная операция с распределением задач
и количественным предупреждением об исчезающих отметках; она в эту
возможность не входит.

#### Scenario: Пустой узел удалён

- **WHEN** Администратор удаляет узел, у которого нет потомков
- **THEN** узел исчезает из дерева
- **AND** его прежний родитель остаётся на месте

#### Scenario: Удаление узла с потомками

- **WHEN** Администратор пытается удалить узел, у которого есть потомки
- **THEN** удаление отклоняется
- **AND** ни сам узел, ни его потомки не исчезают

#### Scenario: Родитель после удаления единственного потомка

- **WHEN** у Раздела с единственным потомком этот потомок удалён
- **THEN** вид прежнего родителя — Тема

#### Scenario: Удаление Темы с Задачами

- **WHEN** Администратор пытается удалить Тему, на которой размечена хотя бы
  одна Задача
- **THEN** удаление отклоняется
- **AND** сообщается, что на Теме есть Задачи
- **AND** ни Тема, ни её Задачи не исчезают

#### Scenario: Удаление Темы, освободившейся от Задач

- **WHEN** с Темы снята разметка последней Задачи и Тема удаляется
- **THEN** Тема исчезает из дерева

### Requirement: Обход поддерева

Система SHALL по любому узлу возвращать этот узел и **всех** его потомков —
на любой глубине, а не только прямых. На этом обходе строится правило «поиск
по узлу включает всё поддерево» (инвариант 10,
[domain-model.md](../../context/domain-model.md)): без него чем глубже
Администратор размечает дерево, тем меньше находится по верхним узлам,
и размечать глубоко перестают.

#### Scenario: Поддерево на четырёх уровнях

- **WHEN** запрашивается поддерево корневого узла ветки глубиной в четыре
  уровня
- **THEN** возвращаются все узлы ветки, включая самые нижние
- **AND** сам запрошенный узел входит в результат

#### Scenario: Поддерево листа

- **WHEN** запрашивается поддерево узла без потомков
- **THEN** возвращается один этот узел

#### Scenario: Соседняя ветка не попадает в результат

- **WHEN** запрашивается поддерево узла, у которого есть брат со своими
  потомками
- **THEN** ни брат, ни его потомки в результат не входят

### Requirement: Экран дерева показывает структуру целиком

Система SHALL показывать всё дерево на одной странице — вложенными списками,
с сохранением уровней вложенности. Ветви SHALL сворачиваться и разворачиваться.
Потомки одного родителя показываются по алфавиту; ручного порядка братьев нет.

Страница SHALL делиться на две части: **слева дерево, справа действия**.
Дерево показывает только структуру — имена узлов и их вложенность. Формы
создания, переименования, перемещения и удаления живут справа и относятся
к **выбранному узлу**; узел выбирается щелчком по нему в дереве.

Причин разделить две. Первая: структура нужна видимой целиком именно потому,
что Администратор её перестраивает — чтобы перенести узел, надо видеть, куда
переносишь, — а формы, вставленные между узлами, прячут ровно то, ради чего
человек пришёл. Вторая: форма перемещения содержит список всех узлов дерева,
и повторённая у каждого узла она растёт квадратично — на сотне узлов это сто
списков по сотне пунктов. В правой части список один.

После любой операции страница SHALL возвращаться к затронутому узлу, а не
к началу дерева, и SHALL оставлять его выбранным: иначе на каждой правке
теряется и место в дереве, и то, над чем Администратор работал.

#### Scenario: Дерево видно целиком

- **WHEN** Администратор открывает экран рубрикатора
- **THEN** показываются все узлы дерева
- **AND** вложенность узлов видна

#### Scenario: Братья по алфавиту

- **WHEN** у родителя есть несколько потомков
- **THEN** они показаны в алфавитном порядке имён

#### Scenario: Действия относятся к выбранному узлу

- **WHEN** Администратор выбирает узел в дереве
- **THEN** справа показываются действия над этим узлом
- **AND** в дереве действий нет — там только имена узлов и их вложенность

#### Scenario: Пока узел не выбран

- **WHEN** Администратор открывает экран, не выбрав узла
- **THEN** дерево показано целиком
- **AND** действий над узлом не предлагается
- **AND** завести корневой узел всё равно можно

#### Scenario: Возврат к выбранному узлу после операции

- **WHEN** Администратор переименовывает узел в глубине дерева
- **THEN** после сохранения страница показывает этот же узел
- **AND** узел остаётся выбранным, и действия над ним доступны сразу

#### Scenario: Выбран узел, которого больше нет

- **WHEN** выбирается узел, удалённый к этому моменту
- **THEN** дерево показывается целиком
- **AND** сообщается, что такого узла нет
- **AND** действий над узлом не предлагается

#### Scenario: Учителю операций не предлагают

- **WHEN** экран рубрикатора открывает Пользователь без роли Администратора
- **THEN** дерево показано
- **AND** выбранный узел показывается со своим путём от корня и видом
- **AND** действий создания, переименования, перемещения и удаления на нём нет
