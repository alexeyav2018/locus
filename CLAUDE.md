# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Статус: кода нет, решения приняты

Прикладного кода нет. Стек выбран, предметная область проработана, решения
записаны в журнале `openspec/context/adr/`, очередь работ — в
`openspec/backlog.md`.

**Не считай, что что-то из описанного уже реализовано.** Документы контекста
описывают замысел; что система действительно умеет — только `openspec/specs/`,
а он пока пуст.

## Продукт

Платформа поддержки учителя математики. **Система — рабочий инструмент учителя;
ученик в ней не пользователь, а объект учёта.** Взаимодействие с учеником
целиком происходит вне системы.

Заявленные заказчиком сценарии и их судьба:

| Сценарий | Состояние |
| --- | --- |
| собирать и систематизировать задачи | в первой версии |
| вести учёт теоретических материалов | в первой версии |
| выдавать задания и вести учёт ответов | в первой версии, но выдача и приём — вне системы |
| выстраивать расписание учителя | отложено ([ADR-0019](openspec/context/adr/0019-raspisanie-otlozheno.md)) |

Смысловое ядро: учитель ведёт **общую библиотеку размеченных задач** и по
каждому ученику знает, **какими методами в каких темах тот владеет**.

## Где что лежит

Не пересказывай эти документы по памяти — читай их.

| Документ | Что в нём | Когда читать |
| --- | --- | --- |
| `openspec/context/glossary.md` | словарь терминов | всегда, перед любым артефактом и кодом |
| `openspec/context/domain-model.md` | сущности, связи, 12 инвариантов | при работе с моделью данных или правами |
| `openspec/context/scenarios.md` | рабочие сценарии учителя | при написании спецификаций |
| `openspec/context/architecture.md` | стек, слои, хранилище, размещение | при проектировании и реализации |
| `openspec/context/adr/` | журнал решений с обоснованием и отвергнутым | когда предложение противоречит принятому |
| `openspec/context/strategy.md` | редакция: горизонт, предпосылки, вехи, не-цели, отложенное | при оценке объёма |
| `openspec/context/standards.md` | правила работы с кодом | при реализации |
| `openspec/context/antipatterns.md` | чего делать нельзя и почему | при проектировании и реализации |
| `openspec/backlog.md` | очередь работ | при выборе следующей задачи |

## Доменный инвариант: граница общего и личного

Главное, что защищает проект от тихой поломки. **Любое предложение обязано
явно сказать: где живут его данные — в общей библиотеке или в личном контуре
учителя, кто имеет право их менять и фильтруются ли они по владельцу.**

```
   ОБЩЕЕ (под Администратором)         ЛИЧНОЕ (у каждого учителя)
   ---------------------------------   ----------------------------
   рубрикатор: Разделы и Темы          Ученики, Группы
   словари Методов и Характеристик     Задания и сроки
   Задачи (два PDF + разметка)         Работы: сканы и вердикты
   Теоретические материалы             Отметки Владения
```

Фильтр по владельцу применяется **только к правой колонке**. Применённый
к библиотеке — сделает её невидимой; забытый на личных данных — покажет
учителю чужих учеников. Обе ошибки тихие.

## Ключевые решения (сводка)

Полностью — в `openspec/context/adr/README.md`. Самое существенное:

- Ученик не пользователь системы ([ADR-0003](openspec/context/adr/0003-sistema-instrument-uchitelya.md)).
- Несколько учителей с первой версии, библиотека общая ([ADR-0004](openspec/context/adr/0004-mnogouchitelskaya-rabota.md)).
- Роли Администратор и Учитель; вход один, функционал объединённый ([ADR-0005](openspec/context/adr/0005-roli-administrator-uchitel.md)).
- Рубрикатор — дерево; задачи и отметки только на листьях ([ADR-0006](openspec/context/adr/0006-rubrikator-derevo.md)).
- Задача — два PDF; формулы как данные не хранятся ([ADR-0008](openspec/context/adr/0008-zadacha-dva-pdf.md)).
- Отметка владения — на паре «Тема × Метод», ставит только человек ([ADR-0011](openspec/context/adr/0011-otmetka-na-pare.md)).
- Агрегат владения — распределение по статусам, не одно значение ([ADR-0013](openspec/context/adr/0013-agregaciya-raspredelenie.md)).
- Автопроверки, уведомлений и фоновых процессов нет ([ADR-0014](openspec/context/adr/0014-proverka-bez-avtomatiki.md), [ADR-0017](openspec/context/adr/0017-net-uvedomlenij.md)).
- Стек: Java 21, Spring, Thymeleaf, PostgreSQL, VPS ([ADR-0020](openspec/context/adr/0020-stek.md)).

## Процесс: OpenSpec

Проект ведётся спекой вперёд кода. Инструмент — OpenSpec (`@fission-ai/openspec`,
запускается через `npx -y @fission-ai/openspec@latest`, короткое имя `openspec`).

Структура:

- `openspec/specs/` — действующие спецификации системы (истина о том, что уже есть);
- `openspec/changes/<имя>/` — предложение изменения: зачем, требования, задачи;
- `openspec/changes/archive/` — выполненные изменения;
- `openspec/context/` — документы проекта: словарь, модель, сценарии,
  архитектура, журнал решений, стандарты;
- `openspec/backlog.md` — приоритизированная очередь работ;
- `openspec/config.yaml` — схема `spec-driven`, правило языка, маршрутизация
  по документам контекста.

**Язык артефактов — русский**, но структурные заголовки OpenSpec и ключевые слова
`SHALL`/`MUST` остаются английскими: по ним идёт валидация (`openspec/config.yaml:3`).

Рабочий цикл — слэш-команды из `.claude/commands/opsx/` (инструкции к ним лежат
в `.claude/skills/openspec-*/SKILL.md`):

| Команда | Что делает |
| --- | --- |
| `/opsx:explore` | разобраться в задаче до написания предложения |
| `/opsx:propose "идея"` | создать новое изменение в `openspec/changes/` |
| `/opsx:update` | доработать уже созданное предложение |
| `/opsx:apply` | реализовать изменение по его задачам |
| `/opsx:sync` | привести спеки в соответствие с кодом |
| `/opsx:archive` | закрыть изменение и обновить `openspec/specs/` |

Полезное из CLI:

```bash
openspec list              # список изменений
openspec list --specs      # список спецификаций
openspec validate          # проверить корректность спек и изменений
openspec show <имя>        # показать изменение или спеку
openspec status            # готовность артефактов по изменению
```

Не начинай реализацию в обход этого цикла: сначала предложение, потом код.

### Ведение бэклога

Метод описан в навыке `backlog-maintenance` — он единственный источник правил
(формат карточек, порядок, процедуры). Здесь только особенность проекта:
поскольку кода ещё нет, очередь состоит из работ по постройке, и порядок задаёт
**зависимость → стоимость → связи → ломающесть**, а не классы `Л/Б/Т/Д`.
Подробности — в разделе 1 самого `openspec/backlog.md`.

### Журнал решений

Записи `openspec/context/adr/` **неизменяемы**. Изменилось решение — пишется
новая запись, у старой правятся только `status` и `superseded_by`. После любой
правки журнала прогнать:

```bash
python3 openspec/context/adr/build_index.py          # пересобрать индекс
python3 openspec/context/adr/build_index.py --check  # только проверить
```

Скрипт проверяет нумерацию, статусы и двусторонность связей между записями.

### Документы проекта — исходники

`openspec/context/` и сам `CLAUDE.md` — **исходники проекта наравне с кодом**,
а не сопроводительные заметки. Правка, меняющая **смысл**, идёт через цикл
изменений; правка формулировки — попутно:

| Через цикл | Попутно, без цикла |
| --- | --- |
| термин, инвариант, не-цель | формулировка, опечатка |
| горизонт, предпосылка, порядок вех | уточнение примера, битая ссылка |

У изменения, правящего только документы, может не быть дельты спек — тогда
в его `.openspec.yaml` ставится `skip_specs: true`.

`openspec/context/strategy.md` версионируется редакциями: редакция не правится
по частям, а заменяется целиком, прежняя уезжает в
`openspec/context/strategy-archive/`. Действующий горизонт, предпосылки и
условия истечения — в самом документе, здесь они не пересказываются.

`openspec/config.yaml` — оснастка, а не исходник: правится напрямую.

Основание — [ADR-0022](openspec/context/adr/0022-kontekst-kak-ishodniki.md).

## MCP-серверы

Подключены в `.mcp.json` (область — проект, файл коммитится):

- **serena** — семантическая навигация и правка кода через языковые серверы:
  поиск символов, ссылок, точечное редактирование вместо чтения файлов целиком.
  Запускается `uvx` из git-репозитория `oraios/serena`, контекст `ide-assistant`.
  Свой кеш кладёт в `.serena/cache/` (в `.gitignore`). Пока кода нет, пользы от
  него мало — он раскроется, когда появится кодовая база.
- **context7** — актуальная документация библиотек и фреймворков по запросу.
  Нужен при работе со Spring и его экосистемой: не полагайся на память о версиях
  API, спроси его.

Проектные MCP-серверы требуют однократного подтверждения пользователем при первом
запуске сессии.

## Команды сборки и тестов

**Ещё не заполнены: кода нет.** Стек выбран
([ADR-0020](openspec/context/adr/0020-stek.md)), команды появятся вместе
с первым элементом бэклога `project-skeleton`, и заполнить этот раздел —
часть его признака готовности: команды сборки, запуска, прогона всех тестов
и прогона одного теста.

## Решения, которые ещё не приняты

Пять вопросов, стоявших здесь раньше, закрыты: стек ([ADR-0020](openspec/context/adr/0020-stek.md)),
представление формул ([ADR-0008](openspec/context/adr/0008-zadacha-dva-pdf.md)),
модель данных ([domain-model.md](openspec/context/domain-model.md)),
автопроверка ([ADR-0014](openspec/context/adr/0014-proverka-bez-avtomatiki.md)),
ученик у нескольких учителей ([ADR-0004](openspec/context/adr/0004-mnogouchitelskaya-rabota.md) — нет, принадлежит одному).

Открытым остаётся:

1. Роль родителя — не обсуждалась вовсе.
2. Возможное возвращение роли ученика — если практика покажет потребность,
   это будет новое решение, а не возврат к [ADR-0001](openspec/context/adr/0001-shkolnik-polnocennyj-polzovatel.md).
3. Что происходит с отметками при правке Методов у размеченной задачи —
   элемент бэклога `method-edit-impact`.

Отложенное с условиями возврата — в `openspec/context/strategy.md`;
технические развилки внутри работ — в карточках `openspec/backlog.md`.

## Правило ведения этого файла

Этот файл **маршрутизирует, а не пересказывает**. Содержательное живёт
в `openspec/context/`; сюда попадает только то, что нужно знать до открытия
любого другого файла. Как только очередное решение принято — оно становится
записью в журнале, а здесь при необходимости правится сводка и список открытых
вопросов.
