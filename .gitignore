# Сборка Maven
target/

# Serena: кеш индексов языковых серверов, пересоздаётся сам
.serena/cache/

# Локальные настройки Claude Code (проектные .claude/ и .mcp.json — коммитим)
.claude/settings.local.json

# IDE
.idea/

# Файлы, положенные в локальное хранилище при разработке (locus.file.local.directory)
.locus-files/
