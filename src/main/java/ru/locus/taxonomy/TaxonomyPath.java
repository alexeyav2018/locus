package ru.locus.taxonomy;

/**
 * Узел, названный полным путём от корня, — для выбора нового родителя
 * при перемещении.
 *
 * Одного имени мало: одинаковые имена под разными родителями законны
 * («признаки подобия» и под треугольниками, и под окружностями), а в списке
 * выбора они были бы неразличимы. Путь собирается в сервисе — в шаблоне
 * вычислений не бывает.
 */
public record TaxonomyPath(TaxonomyNodeId id, String path) {
}
