package ru.locus.work;

import java.net.URI;

/**
 * Файл Работы вместе с временной ссылкой на его содержимое — то, что
 * уходит на экран.
 *
 * Ключ хранилища в разметку не попадает: сканы работ — персональные данные
 * детей, и единственный путь к содержимому — подписанная ссылка, живущая
 * минуты (ADR-0021). Ссылка считается при каждом показе, а не хранится:
 * это свойство хранилища, не Работы.
 */
public record LinkedFile(StudentWorkFile file, URI link) {
}
